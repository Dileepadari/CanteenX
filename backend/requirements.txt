# --- Web framework -----------------------------------------------------------
fastapi==0.115.12
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- GraphQL -----------------------------------------------------------------
strawberry-graphql[fastapi]==0.262.5
graphql-core==3.2.6

# --- Database ----------------------------------------------------------------
SQLAlchemy[asyncio]==2.0.39
asyncpg==0.30.0
greenlet==3.1.1
alembic==1.15.1

# --- Settings & validation ---------------------------------------------------
pydantic==2.10.6
pydantic-settings==2.7.1
python-dotenv==1.0.0

# --- Auth --------------------------------------------------------------------
# argon2 is the password hash; bcrypt stays only to verify pre-existing hashes
# during the one-way rehash-on-login migration (see core/security.py).
PyJWT==2.10.1
argon2-cffi==23.1.0
bcrypt==4.2.1
python-cas==1.6.0

# --- Payments ----------------------------------------------------------------
razorpay==1.4.2
# razorpay 1.4.2 imports pkg_resources at module load. Python 3.12 venvs no
# longer ship setuptools by default, and setuptools >= 81 removed
# pkg_resources outright - so this needs to be present *and* held below 81.
# Revisit when razorpay drops the import.
setuptools>=75.0.0,<81

# --- Uploads / outbound HTTP -------------------------------------------------
httpx==0.28.1
Pillow==11.1.0

# --- Observability & limits --------------------------------------------------
python-json-logger==3.2.1
limits==4.0.1
